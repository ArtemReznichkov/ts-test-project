import React from 'react';

import PageHeader from '../../shared/PageHeader';

const AdsPage: React.FC = () => (
    <div>
        <PageHeader title="Ads Page" />
    </div>
);

export default AdsPage;
